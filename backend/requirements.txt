# FastAPI Backend Dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Database
aiosqlite>=0.20.0

# File handling
aiofiles>=23.2.0
Pillow>=10.0.0
python-magic>=0.4.27

# AI / Google
google-generativeai>=0.5.0

# HTTP client
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============================================================
# Local Health AI models — MedGemma (text + multimodal image) and
# MedSigLIP (image classification) load via transformers; voice
# transcription uses Whisper (non-gated, Apache-2.0) by default.
# Everything is feature-flagged in config (MEDGEMMA_MODE / MEDSIGLIP_MODE
# / MEDASR_MODE): the app runs in safe STUB mode when weights or these
# deps are absent. CPU / Apple-Silicon (MPS) friendly — no bitsandbytes
# on macOS. See MODELS_SETUP.md for the one-time download steps.
# ============================================================
torch>=2.4.0
transformers>=4.52,<5
accelerate>=0.34.0
huggingface_hub[cli]>=0.34.0
safetensors>=0.4.3
sentencepiece>=0.2.0
protobuf>=3.20.0
numpy<2.3
# Medical image (DICOM) decode + audio I/O for the image & voice agents
pydicom>=2.4.0
librosa>=0.10.2
soundfile>=0.12.1
# CUDA-only 4-bit quantization (Linux GPU boxes only; skipped on macOS/CPU)
bitsandbytes>=0.43.0 ; platform_system == 'Linux'
